apiVersion: v1
kind: Service
metadata:
  name: kafka-service-broker-service
  namespace: catalog
spec:
  selector:
    app: kafka-service-broker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
